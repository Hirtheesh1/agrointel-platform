# ──────────────────────────────────────────────────────────────────
# requirements-prod.txt  –  Render / production-only dependencies
#
# Intentionally excludes:
#   • test libs (pytest, respx, pytest-mock, …)
#   • heavy ML training libs (torch, pytorch-lightning,
#     pytorch-forecasting, darts, tensorboard)
#     → these are only needed for model training, not for serving
#       the API.  Run training offline and commit model artefacts,
#       or use a separate Render background-worker with a GPU plan.
#   • aiosqlite  (SQLite driver – we use PostgreSQL in production)
# ──────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Validation / settings
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg[binary]==3.1.18

# Utilities
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4
httpx==0.27.0
tenacity==8.3.0
apscheduler==3.10.4

# Data / ML inference (lightweight; pre-built wheels exist for Python 3.11)
pandas==2.2.2
numpy==1.26.4
scikit-learn>=1.3.0
polars>=0.19.0
shap>=0.42.1
